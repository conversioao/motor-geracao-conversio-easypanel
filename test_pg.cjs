const { Client } = require('pg'); const c = new Client({ connectionString: process.env.DATABASE_URL || 'postgresql://neon_ai_db:password@localhost/conversio' }); c.connect().then(() => { return c.query(INSERT INTO generations (user_id, type, prompt, cost, status, metadata, batch_id, model, style, aspect_ratio) VALUES ('test', 'image', 'test', 1, 'processing', '{}', 'test', 'test', 'test', '1:1')); }).then(() => console.log('success')).catch(e => console.log('FULL ERROR:', e)).finally(() => process.exit(0));
